# convex/

Convex backend — source of truth for all ticket and queue state
(`tech_design.md` §1). Schema lands in TASK-5–7; functions follow per TASKS.md.

Generated `_generated/` types appear after the first `npx convex dev` and are
gitignored-by-convention (committed in many setups, but omitted here until a
deployment exists — see WAITING_ON_HUMAN.md for the Convex deploy key).
