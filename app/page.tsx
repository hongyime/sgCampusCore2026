export default function HomePage() {
  return (
    <main style={{ padding: "2rem", fontFamily: "system-ui, sans-serif" }}>
      <h1>CampusCore</h1>
      <p>
        Public dashboard scaffold. Real-time ticket list, TTR/SBL metrics, and
        the campus-health breakdown are built in later tasks (see TASKS.md).
      </p>
    </main>
  );
}
